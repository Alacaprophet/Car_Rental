@model Domain.Entities.RentalPeriod

@{
    ViewData["Title"] = "Delete";
    Response response = ViewBag.Response;
}

<h1>Sil</h1>
<hr />
@if (response != null)
{
    <message-box is-success="@response.IsSuccess" message="@response.Message"></message-box>
}
<div>
    @if (response == null)
    {
        <h4 class="text-warning">@Model.Name kiralama periyodunu silmek istediğinize emin misiniz?</h4>
    }
    <hr />
    <form asp-action="Delete">
        <dl class="row">
            @*<dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Id)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Id)
                </dd>*@
            @Html.HiddenFor(m => m.Id)
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Name)
            </dd>
        </dl>


        <input type="submit" value="Sil" class="btn btn-danger" /> |

        <a asp-action="Index">Kiralama Periyodu Listesine Geri Dön</a>
    </form>
</div>
